# Plain Secret with a fake DSN — demo only.
# For real use: swap for SOPS-encrypted, SealedSecrets, or ExternalSecrets+AWS SM.
apiVersion: v1
kind: Secret
metadata:
  name: app-db
stringData:
  DATABASE_URL: "${DATABASE_URL}"
