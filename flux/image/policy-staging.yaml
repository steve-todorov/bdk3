apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: app-staging
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: app
  filterTags:
    pattern: '^main-(?P<build>\d+)-\d+$'
    extract: '$build'
  policy:
    numerical:
      order: asc
